#cooperates with util.py
#If your util.py file is in a different directory, you might need to add that directory to the system path:

import sys
sys.path.insert(0, '/path/to/directory')
import util

Replace '/path/to/directory' with the actual path to the directory containing util.py.
